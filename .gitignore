node_modules/
dist/
.DS_Store

# Android
build/
.gradle/
local.properties
*.apk
*.aab
.cxx/
captures/

# 검증 산출물
tools/out/

# 데모 웹 빌드 — web/ 를 그대로 복사한 것이라 커밋하면 저장소가 두 배가 되고,
# web/ 을 고칠 때마다 100개 넘는 파일이 diff 에 뜬다. pages.yml 이 배포 직전에 굽는다.
#   node tools/demo-build.mjs   (직접 볼 때)
site/play/
