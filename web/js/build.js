/**
 * 빌드 종류 — 이 파일 하나가 "전체판인가 데모인가"를 가른다.
 *
 * 기본은 전체판(`DEMO = false`). `tools/demo-build.mjs` 가 데모를 구울 때 이 파일을
 * **통째로 덮어쓴다** — 소스를 잘라 고치지 않는다(한 줄이라도 어긋나면 조용히 틀린 빌드가 나온다).
 *
 * 데모에서 달라지는 것은 둘뿐이다:
 *   1. 유료 콘텐츠가 아예 없다 — content/index.js 의 `// demo:strip` 줄과 그 파일들을 빼고 굽는다
 *   2. 결제가 없다 — domain/billing.js 가 DisabledBillingProvider 를 고른다(데모 결제로도 못 연다)
 *
 * 왜 데모가 필요한가: 아이폰에는 앱이 없어서 웹앱(PWA)이 유일한 길인데, 지금 웹 빌드를 그대로
 * 올리면 '데모 결제'가 3막·스킨을 공짜로 열어 준다. 파는 물건을 나눠 주는 셈이라 그럴 수 없다.
 * 자세한 것은 docs/아이폰.md.
 */

export const DEMO = false

/**
 * 공식 사이트 주소 — 데모에서 "전체판은 여기" 라고 가리킬 곳이고,
 * **이 프로젝트에서 사이트 주소를 적어 둔 단 하나의 자리다 (R).**
 *
 * 전에는 `tools/demo-build.mjs` 에도 같은 문자열이 손으로 적혀 있었고 둘이 어긋나지 않게
 * 검사를 하나 붙여 뒀었다. 지금은 그 파일이 여기서 읽어 간다 — 어긋날 자리가 없다.
 * 데모 매니페스트의 `id`(`DEMO_SCOPE`)도 여기 경로에서 뽑는다.
 *
 * ── 도메인을 붙일 때 (사용자가 "주소가 너무 적나라하다"고 해서 준비해 둔 것) ─────────────
 * 지금 주소에는 GitHub 계정 이름이 그대로 들어 있다. 도메인을 사면 이렇게 바꾼다:
 *   1. 이 줄을 `'https://<도메인>/'` 으로 바꾼다 ← **코드에서 고칠 곳은 이 한 줄뿐이다**
 *   2. `site/CNAME` 에 도메인을 한 줄로 적는다 (pages.yml 이 그 파일을 확인한다)
 *   3. DNS 에 **A 레코드 넷**을 넣는다 — 185.199.108~111.153, apex(`@`) 에.
 *      `www` 서브도메인에 CNAME 으로 붙이면 DNS 조회에 계정 이름이 그대로 남는다. 그러면 뜻이 없다
 *   4. README·docs 의 주소를 새 것으로 (거기는 사람이 읽는 글이라 코드에서 안 온다)
 * `start_url`·`scope` 는 상대 경로라 `/-catpaw-defense/` → `/` 로 내려와도 그대로 돈다.
 */
export const FULL_APP_URL = 'https://kimd10041004-gif.github.io/-catpaw-defense/'
