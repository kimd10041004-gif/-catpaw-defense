# 구축내역 — 캣포 디펜스

## 배경

"타워디펜스 안드로이드 게임 하나 만들어줘. 고양이로. 앱으로 구동가능하게" +
"나중에 추가도 가능하게, 추가 설정도 가능하게" 요청으로 만들었다.

작업 환경에 **안드로이드 SDK가 없고 `dl.google.com`이 차단**(`CONNECT tunnel failed, 403`)돼 있어
이 컨테이너에서 APK를 컴파일할 수 없었다. 그래서 "앱으로 구동"을 두 경로로 나눴다.

1. **PWA** — 지금 폰에서 바로 홈 화면에 추가하면 전체화면·오프라인으로 앱처럼 돈다
2. **네이티브 WebView 래퍼 + GitHub Actions** — 러너에는 SDK가 있으므로 CI가 진짜 APK를 만들어 준다

"나중에 추가 가능"이라는 요구가 설계를 결정했다. 콘텐츠를 전부 데이터로 빼고
레지스트리 + 부팅 시 참조 무결성 검사를 넣어, 새 고양이·적·맵·능력·설정을 추가할 때
`game.js` / `render.js` / `ui.js`를 건드리지 않아도 되게 만들었다.

## 구성

```
web/js/domain/     DOM 무의존 순수 로직 (balance, waves, economy, path, targeting, status, settings, save)
web/js/content/    레지스트리 + 데이터 (towers, enemies, maps, waveSets, effects)
web/js/            sprites, render, game, ui, audio, main
tests/node/        단위 테스트 9개 파일 — node --test, 의존성 0
tools/             make-icons.mjs (아이콘 생성), screenshot.mjs (헤드리스 실행 검증)
android/           Kotlin + WebViewAssetLoader 래퍼
.github/workflows/ CI — 단위 테스트 후 APK 빌드·업로드
```

핵심 설계 결정 세 가지.

**좌표계를 전부 타일 단위로 통일했다.** 렌더러만 `tileSize`를 곱한다.
덕분에 경로·사거리·타겟팅 로직이 화면 크기와 DPR을 전혀 모르고, 브라우저 없이 순수 함수로 테스트된다.

**타워 능력을 `if`문이 아니라 핸들러 레지스트리로 처리한다.**
`registerEffect('burn', { onHit })` 한 줄과 타워 데이터 한 줄이면 새 능력이 붙는다.
`onFire`가 `{ consumed: true }`를 반환하면 투사체를 만들지 않는데, 뚱냥의 광역기가 이 방식으로 동작한다.

**설정 화면을 스키마에서 생성한다.** `SETTINGS_SCHEMA` 배열에 한 줄 넣으면 UI가 자동으로 생긴다.
도감도 마찬가지로 레지스트리를 순회하므로 콘텐츠를 추가하면 저절로 나타난다.

안드로이드 래퍼는 `file://`이 아니라 **`WebViewAssetLoader`**로 assets를
`https://appassets.androidplatform.net/` 에 서빙한다. `file://` 출처에서는 ES 모듈 import가 CORS로 막히고
localStorage와 서비스 워커도 불안정하기 때문이다. 이 결정이 웹 쪽 모듈 구조를 지탱한다.
인터넷 권한은 선언하지 않는다 — 모든 파일이 APK 안에 있다.

## 검증 (명령 출력 인용)

### 단위 테스트

```
$ npm test
# tests 129
# suites 0
# pass 129
# fail 0
# cancelled 0
# skipped 0
# todo 0
# duration_ms 352.82209
```

실패 0건.

검사 항목: 방어력 관통과 최소 피해 1 보장, 웨이브·맵·난이도 체력 스케일링, 업그레이드 누적가와
만렙 `null` 반환, 판매금이 항상 투자금보다 작음, 경로 전개와 대각선 웨이포인트 거부, 건설 가능 판정,
타겟팅 4모드와 공중/지상 필터, 슬로우 중첩 규칙(약한 효과가 강한 효과를 덮어쓰지 않음),
설정 스키마 강제 보정, 저장 마이그레이션과 손상 데이터 백업, 레지스트리 참조 무결성.

### 콘텐츠 무결성과 밸런스 곡선

```
validateAll() => {"towers":5,"enemies":6,"maps":3,"waveSets":1,"effects":3,"sprites":5}
  골목길: 경로 33타일 / 건설 가능 94칸 / 난이도 x1
  부엌: 경로 41타일 / 건설 가능 84칸 / 난이도 x1.15
  지붕: 경로 46타일 / 건설 가능 79칸 / 난이도 x1.35
  웨이브별 총 체력: 1w=256 / 5w=1212 / 10w=5194 / 15w=8280 / 20w=15800 / 25w=22508 / 30w=48526
```

밸런스 회귀를 실제로 한 번 잡았다. 21웨이브(바퀴 떼)와 26웨이브의 총 체력이 각각 16·21웨이브보다
낮아 난이도가 역행했다. `content.test.mjs`의 "5웨이브 단위로 항상 증가" 검사가 잡아냈고
두 웨이브에 두더지와 시궁쥐를 보강해 해소했다.

### 실행 검증 (헤드리스 크로미움, 412×915 모바일 뷰포트)

```
실행 검증 결과
  ✓ 콘텐츠 검증 통과 후 부팅 — 고양이 5종 · 해충 6종 · 맵 3종 준비 완료
  ✓ 맵 3개가 표시되고 첫 맵만 열려 있다 — 카드 3개, 잠김 2개
  ✓ 게임이 시작 상태로 초기화된다 — {"gold":300,"lives":20,"waves":30}
  ✓ 캔버스에 맵이 실제로 그려진다 — 고유 색상 34종
  ✓ 경로에 인접한 건설 가능 타일을 찾았다 — 6곳
  ✓ 탭으로 고양이를 배치하고 골드가 차감된다 — {"towers":1,"gold":220}
  ✓ 경로 위 배치는 거부되고 안내가 뜬다 — 여기엔 지을 수 없습니다
  ✓ 골드가 모자라면 배치가 거부된다 — 골드가 부족합니다
  ✓ 두 번째 고양이도 배치된다 — 골드 90
  ✓ 타워를 탭하면 상세 패널이 열린다 — 치즈냥Lv.1/3✕공격력 12 → 20사거리 2.6 → 2.9연사 1.6
  ✓ 업그레이드가 반영된다
  ✓ 웨이브가 시작된다
  ✓ 1웨이브의 적을 모두 처치하고 준비 단계로 돌아온다 — {"phase":"prep","killed":8,"leaked":0,"gold":112,"lives":20}
  ✓ 여러 웨이브를 연속 진행해도 시뮬레이션이 유지된다 — {"wave":9,"enemies":1,"towers":12,"lives":20,"killed":145}
  ✓ 전투 중 화면에 적이 살아 있다 — 적 1마리
  ✓ 설정 화면이 스키마에서 자동 생성된다 — 13개 항목
  ✓ 설정 변경이 localStorage에 즉시 저장된다 — sfx=false
  ✓ 도감이 레지스트리에서 자동 생성된다 — 고양이 5종
  ✓ 도감 해충 탭도 자동 생성된다 — 해충 6종
  ✓ 매니페스트가 세로 전체화면으로 설정돼 있다 — 캣포 디펜스 / 아이콘 3개
  ✓ 서비스 워커가 등록된다 (오프라인 구동)
  ✓ 콘솔 에러 0건 — 없음

통과: 전 항목 통과
스크린샷: /home/user/catpaw-defense/tools/out
```

시뮬레이션만 돌린 게 아니라 **실제 탭 좌표로 조작**했다. 상점 카드를 누르고 캔버스의 타일 좌표를
계산해 `page.mouse.click`으로 배치했으며, 경로 위 배치 거부와 골드 부족 거부도 같은 경로로 확인했다.

스크린샷: `tools/out/1-title.png` … `6-codex.png`

### 서브경로 서빙 (안드로이드와 같은 조건)

안드로이드 앱은 게임을 사이트 루트가 아니라 `/assets/index.html`에 서빙한다.
어딘가에 절대경로가 섞이면 웹은 멀쩡한데 APK만 빈 화면이 되므로 같은 조건을 재현해 확인했다.

```
$ node tools/subpath-check.mjs
  ✓ 서브경로에서 부팅 — 고양이 5종 · 해충 6종 · 맵 3종 준비 완료
  ✓ 서브경로에서 게임 진입 — 골목길 / 골드 300
  ✓ 서비스 워커 스코프 — http://127.0.0.1:37177/assets/
  ✓ 콘솔 에러 0건
```

서비스 워커 스코프가 `/assets/`로 잡히는 것까지 확인했다. 이것이 안드로이드 쪽에서
실제로 검증할 수 있었던 유일한 항목이다 (나머지는 SDK가 없어 불가).

## 비고

### 검증하지 못한 것 — 솔직히

- **안드로이드 Kotlin/Gradle 코드는 이 환경에서 컴파일 검증이 불가능하다.** SDK가 없고 다운로드도 막혀 있다.
  XML 9개가 well-formed인 것까지만 확인했고, 실제 빌드 성공 여부는 **CI가 처음 돌아야 알 수 있다.**
  AGP 8.10.1 / Kotlin 2.1.21 / Gradle 8.14.3 / compileSdk 35 조합도 문서 기준으로 고른 것이지 실측이 아니다.
- **실기기 터치감과 성능은 확인할 수 없다.** 에뮬레이터가 없다. 헤드리스 브라우저 렌더링까지가 한계다.
- **재미는 검증 대상이 아니다.** 밸런스는 수치 설계와 자동 스모크 기준이며, 직접 플레이한 뒤
  조정이 필요할 것이다. 다만 전부 데이터라 `content/` 안의 숫자만 고치면 된다.

### 남겨둔 확장 지점

`dot`(지속 피해) · `chain`(연쇄) · `buff`(주변 타워 강화) · `stun` · `pierce` 는
효과 핸들러 계약만 열어두고 구현하지 않았다. `registerEffect` 한 줄로 붙일 수 있다.

### Gradle wrapper

`gradle wrapper`가 플러그인 해석 때문에 네트워크를 타지 않도록, 빈 시드 프로젝트에서 생성해
`android/`로 복사했다. `gradle-wrapper.jar` 포함 정상 생성됐다.
