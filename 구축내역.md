# 구축내역 — 캣포 디펜스

## 배경

"타워디펜스 안드로이드 게임 하나 만들어줘. 고양이로. 앱으로 구동가능하게" 로 시작해서
"나중에 추가도 가능하게, 추가 설정도 가능하게" → "높은 등급의 보스몹, 특수 공격, 화려한 임팩트,
정말 최소한의 과금 요소" 로 이어진 요청을 구현했다.

작업 환경 제약이 설계를 크게 좌우했다. **안드로이드 SDK가 없고 `dl.google.com`이 차단**
(`CONNECT tunnel failed, 403`)돼 있다. `maven.google.com`도 결국 같은 호스트로 리다이렉트되므로
AGP조차 받을 수 없다. **이 컨테이너에서 APK 컴파일은 물리적으로 불가능**하고,
GitHub Actions가 유일한 경로다.

그래서 "앱으로 구동"을 두 갈래로 보장했다.

1. **PWA** — 지금 폰에서 홈 화면에 추가하면 전체화면·오프라인으로 앱처럼 돈다
2. **네이티브 WebView 래퍼 + CI** — 러너에는 SDK가 있으므로 CI가 진짜 APK를 만든다

"나중에 추가 가능"이라는 요구가 구조를 결정했다. 콘텐츠를 전부 데이터로 빼고 레지스트리와
부팅 시 참조 무결성 검사를 넣어, 새 고양이·적·보스능력·필살기·맵·설정·상품을 추가할 때
`game.js` / `render.js` / `ui.js`를 건드리지 않아도 되게 만들었다.
실제로 2차 요청(보스·필살기·과금)을 구현할 때 이 구조가 그대로 먹혔다 —
보스 능력 6종과 필살기 4종은 각각 `enemyAbilities.js` / `specials.js` 파일 하나씩으로 끝났다.

## 구성

```
web/js/domain/     DOM 무의존 순수 로직
                   balance(크리티컬 포함) waves economy(캣닢) path targeting status
                   settings save(v2) shop billing
web/js/content/    registry + towers enemies enemyAbilities specials maps waveSets effects
web/js/            sprites render game ui audio main
tests/node/        단위 테스트 11개 파일 — node --test, 의존성 0
tools/             make-icons / screenshot(실행검증) / subpath-check(안드로이드 경로 회귀)
android/           Kotlin + WebViewAssetLoader 래퍼 + BillingBridge
docs/              확장가이드.md (15장) · 결제연동.md
```

### 핵심 설계 결정

**좌표계를 전부 타일 단위로 통일했다.** 렌더러만 `tileSize`를 곱한다. 덕분에 경로·사거리·
타겟팅·보스 능력이 화면 크기와 DPR을 전혀 모르고, 브라우저 없이 순수 함수로 검증된다.

**능력을 `if`문이 아니라 핸들러 레지스트리로 뺐다.** 타워 능력(`registerEffect`)과
보스 능력(`registerEnemyAbility`)이 같은 패턴이다. `onDamaged`가 숫자를 반환하면 피해량을
대체하는데, 보호막이 이 계약 하나로 구현된다. `onFire`가 `consumed`를 반환하면 투사체를
만들지 않는데, 뚱냥의 광역기가 이 방식이다.

**오라 스탯은 매 스텝 초기화한다.** 전투 함성처럼 다른 적을 강화하는 능력은 `e.auraArmor` /
`e.auraSpeed`에만 쓰고, 매 스텝 0/1로 리셋한 뒤 능력 틱을 이동보다 먼저 돌린다.
`e.def`를 직접 고치면 그 정의가 다음 판까지 오염되기 때문이다.

**설정과 상점 UI는 데이터에서 생성한다.** `SETTINGS_SCHEMA`와 `CATNIP_ITEMS`/`IAP_PRODUCTS`
배열을 순회해 DOM을 만든다. 항목을 늘려도 UI 코드는 그대로다.

**결제는 제공자를 추상화했다.** 웹은 `MockBillingProvider`, 안드로이드는 `AndroidBillingProvider`가
`window.CatpawBilling` 브리지를 쓴다. 브리지는 지금 `configured = false`라 결제가
**성공한 척하지 않고 `not_configured`를 그대로 돌려준다.**

**안드로이드는 `file://`이 아니라 `WebViewAssetLoader`**로 assets를
`https://appassets.androidplatform.net/assets/`에 서빙한다. `file://` 출처에서는 ES 모듈 import가
CORS로 막히고 localStorage·서비스워커도 불안정하다. 인터넷 권한은 선언하지 않는다.

## 검증 (명령 출력 인용)

### 단위 테스트

```
$ npm test
# tests 171
# suites 0
# pass 171
# fail 0
# cancelled 0
# skipped 0
# todo 0
# duration_ms 411.561464
```

검사 항목: 방어력 관통과 최소 피해 1, **크리티컬이 방어력보다 먼저 곱해지는 순서**,
웨이브·맵·난이도 체력 스케일링, 업그레이드 누적가와 만렙 `null`, 판매금 < 투자금,
경로 전개와 대각선 거부, 타겟팅 4모드와 공중/지상 필터, 슬로우 중첩 규칙,
설정 스키마 강제 보정, **세이브 v0→v1→v2 마이그레이션과 손상 데이터 백업**,
레지스트리 참조 무결성(미등록 스프라이트·효과·보스능력·필살기·소환 대상),
**보스 등급 사다리**, **결제 영수증 중복 지급 방지**, **결제 없이 이어하기 값이 모이는지**.

### 콘텐츠 무결성과 보스 사다리

```
validateAll() => {"towers":5,"enemies":10,"maps":4,"waveSets":2,"effects":3,"sprites":5,"enemyAbilities":6,"specials":4}
보스 사다리:
  10웨이브: 쥐왕 (총 체력 5194)
  15웨이브: 두더지 대장 (총 체력 11160)
  20웨이브: 쥐왕, 바퀴 여왕 (총 체력 21890)
  25웨이브: 흡혈 박쥐왕, 두더지 대장 (총 체력 29648)
  30웨이브: 마왕 쥐, 쥐왕, 두더지 대장 (총 체력 82846)
```

### 실행 검증 (헤드리스 크로미움, 412×915 모바일 뷰포트)

```
실행 검증 결과
  ✓ 콘텐츠 검증 통과 후 부팅 — 고양이 5종 · 해충 10종 · 맵 4종 준비 완료
  ✓ 맵 4개가 표시되고 첫 맵만 열려 있다 — 카드 4개, 잠김 3개
  ✓ 게임이 시작 상태로 초기화된다 — {"gold":300,"lives":20,"waves":30}
  ✓ 캔버스에 맵이 실제로 그려진다 — 고유 색상 28종
  ✓ 경로에 인접한 건설 가능 타일을 찾았다 — 6곳
  ✓ 탭으로 고양이를 배치하고 골드가 차감된다 — {"towers":1,"gold":220}
  ✓ 경로 위 배치는 거부되고 안내가 뜬다 — 여기엔 지을 수 없습니다
  ✓ 골드가 모자라면 배치가 거부된다 — 골드가 부족합니다
  ✓ 두 번째 고양이도 배치된다 — 골드 90
  ✓ 타워를 탭하면 상세 패널이 열린다 — 치즈냥Lv.1/3✕공격력 12 → 20사거리 2.6 → 2.9연사 1.6
  ✓ 업그레이드가 반영된다
  ✓ 웨이브가 시작된다
  ✓ 1웨이브의 적을 모두 처치하고 준비 단계로 돌아온다 — {"phase":"prep","killed":8,"leaked":0,"gold":112,"lives":20}
  ✓ 필살기 버튼이 등록된 수만큼 생성된다 — 4개
  ✓ 필살기 검증용으로 적이 전장에 남아 있다 — 9마리
  ✓ 필살기가 적 체력을 실제로 깎고 쿨다운에 들어간다 — 총 체력 1078 → 0, 화면섬광 0.19
  ✓ 여러 웨이브를 연속 진행해도 시뮬레이션이 유지된다 — {"wave":9,"enemies":6,"towers":12,"lives":20,"killed":126}
  ✓ 전투 중 화면에 적이 여럿 살아 있다 — 적 6마리
  ✓ 최종 보스가 등장하고 보호막을 두른다 — {"name":"마왕 쥐","tier":3,"shield":10175,"shieldMax":10530,"abilities":["shield","regen","summon","enrage","warcry"],"onField":83,"summoned":34}
  ✓ 보스가 부하를 실제로 소환한다 — 소환된 시궁쥐 34마리 / 전장 83마리
  ✓ 설정 화면이 스키마에서 자동 생성된다 — 13개 항목
  ✓ 설정 변경이 localStorage에 즉시 저장된다 — sfx=false
  ✓ 상점이 상품 배열에서 자동 생성된다 — 6개 상품
  ✓ 데모 결제임을 숨기지 않고 표시한다 — 결제 방식: 데모 결제 (실제 청구 없음)
  ✓ 결제가 캣닢을 지급하고 영수증과 함께 저장된다 — 32 → 132, 영수증 1건
  ✓ 캣닢으로 산 소모품이 즉시 적용되고 캣닢이 차감된다 — 골드 2858 → 3258, 캣닢 132 → 102
  ✓ 도감이 레지스트리에서 자동 생성된다 — 고양이 5종
  ✓ 도감 해충 탭도 자동 생성된다 — 해충 10종
  ✓ 매니페스트가 세로 전체화면으로 설정돼 있다 — 캣포 디펜스 / 아이콘 3개
  ✓ 서비스 워커가 등록된다 (오프라인 구동)
  ✓ 콘솔 에러 0건 — 없음

통과: 전 항목 통과
스크린샷: /home/user/catpaw-defense/tools/out
```

시뮬레이션만 돌린 게 아니라 **실제 탭 좌표로 조작**했다. 상점 카드를 누르고 캔버스 타일
좌표를 계산해 `page.mouse.click`으로 배치했으며, 필살기 버튼 클릭·데모 결제·캣닢 소모품
구매까지 전부 실제 DOM 경로를 통과시켰다.

### 서브경로 서빙 (안드로이드와 같은 조건)

```
$ node tools/subpath-check.mjs
✓ 서브경로에서 부팅 — 고양이 5종 · 해충 10종 · 맵 4종 준비 완료
  ✓ 서브경로에서 게임 진입 — 골목길 / 골드 300
  ✓ 서비스 워커 스코프 — http://127.0.0.1:41891/assets/
  ✓ 콘솔 에러 0건
```

안드로이드 앱은 게임을 사이트 루트가 아니라 `/assets/index.html`에 서빙한다. 절대경로가
하나라도 섞이면 웹은 멀쩡한데 APK만 빈 화면이 되므로 같은 조건을 재현해 확인했다.
**안드로이드 경로에서 실제로 검증할 수 있었던 유일한 항목이다.**

## UI 재작업 · 조작 수정 (2차)

"조작에 이질감이 들고 UI를 상업용 수준으로" 라는 지적에 따라 다시 손봤다.

### 조작 — 원인은 대부분 내 코드의 버그였다

**① 닫은 타워 패널이 지도를 계속 덮고 있었다 (가장 큰 원인).**
`hideTowerPanel()` 은 `hidden = true` 만 걸었는데, `.tower-panel { display: flex }` 라는
작성자 스타일이 브라우저 기본 `[hidden] { display: none }` 을 덮어쓴다.
**작성자 스타일은 기본 스타일보다 항상 우선한다** — 그래서 패널은 한 번 열린 뒤로 계속
화면에 남아 아래쪽 약 200px 의 탭을 전부 삼켰다. 타워를 한 번 눌러본 뒤로는 지도 아래쪽이
통째로 먹통이었다는 뜻이다. 규칙마다 챙기지 않도록 전역 `[hidden] { display: none !important }`
한 줄로 못 박고, 스모크에 "패널을 닫으면 그 자리에서 캔버스가 잡힌다" 회귀 검사를 넣었다.

**② 밀어서 넘긴 손가락까지 '탭'으로 처리했다.** `pointerdown` 에 위치를 적어두는 코드는
있었는데(`_pressPoint`) 아무 데서도 읽지 않는 죽은 코드였다. 이동 12px · 700ms 안쪽일 때만
탭으로 보도록 실제로 판정하게 고쳤다.

**③ 진동(햅틱) 설정이 아무 일도 하지 않았다.** 스키마에 항목만 있고 `navigator.vibrate` 를
부르는 코드가 어디에도 없었다. 배치·업그레이드·판매·목숨 누출에 붙이고, 설정을 끄면 실제로
호출이 멈추는지 스모크로 확인한다.

**④ 판매 확인이 OS 기본 `window.confirm` 이었다.** 전체화면 게임에서 시스템 대화상자가
튀어나오는 게 이질감의 전형이다. 게임 안 시트로 바꿨다.

**⑤ 배치 모드를 빠져나갈 방법이 없었다.** 무엇을 짓는 중인지 + 취소 버튼을 항상 띄운다.
안내 표시는 `placingId` 에서 파생시켜(`_syncPlacingHint`) 상태가 어긋날 수 없게 했다 —
켜고 끄는 지점이 여러 곳이면 반드시 한 군데가 빠지고, 남은 유령 안내가 다시 탭을 삼킨다.

### UI — 웹 폼이 아니라 게임으로

- **디자인 시스템 도입**: 표면 4단계(`--bg-0` ~ `--surf-3`) · 그림자 3단계 · 안쪽 하이라이트.
  평평한 단색 대신 위에서 빛이 오는 그라디언트를 쓴다.
- **버튼을 눌리는 물체로**: 아래 그림자(`0 3px 0`)가 두께이고, 누르면 그만큼 실제로 내려앉는다.
- **이모지 → 인라인 SVG 아이콘 16종**. 이모지는 기기·폰트에 따라 모양·크기·색이 달라지고,
  컬러 이모지 폰트가 없는 환경에서는 흑백 윤곽으로 뜬다. 필살기 아이콘도 `svg:` 접두사로
  바꿨다(문자열 이모지도 계속 쓸 수 있게 호환은 유지).
- **HUD**: 숫자 캡슐 + tabular-nums, 골드가 늘면 튕기고 목숨이 깎이면 흔들린다.
- **웨이브 바**: `WAVE 9 / 30 · 남은 해충 7` 처럼 상태를 직접 읽어준다.
- **타워 패널**: 썸네일 + 2열 스탯 그리드. 버튼 안 금액은 아이콘 없이 숫자만
  (작은 동전은 그냥 점으로 보인다). 표적 버튼은 `표적 / 선두` 두 줄.
- **바닥과 길**: 옅은 격자선 + 위에서 오는 방사형 조명 + 길 아래 그림자.
- **떠오르는 숫자 정리**: 같은 적에게 들어간 피해와 짧은 시간 안의 골드를 각각 하나로 합친다.
  연사 타워 여럿이 한 적을 때리면 `+9` 가 수십 개 겹쳐 글자 덩어리가 됐다.
  필살기 안내는 전용 줄 하나에만 띄운다.

### 번들러가 아이콘을 통째로 잘라먹고 있었다

단일 파일 번들은 `index.html` 을 `<div id="app">` 부터 잘라 붙였는데, 아이콘 정의인
`<svg id="icon-defs">` 는 그 앞에 있어서 전부 빠졌다. **오류는 안 나고 아이콘만 빈칸이 된다.**
`<body>` 전체를 가져오도록 고치고, 번들 시점에 "참조하는 아이콘이 정의와 함께 실려 있는지"
검사하는 가드와 스모크의 실제 렌더 검증(`9-bundle.png`)을 함께 넣었다.

### 검증

```
node --test  →  177/177 통과
tools/screenshot.mjs  →  39/39 통과, 콘솔 에러 0건
  ✓ 패널을 닫으면 지도 위에서 완전히 사라진다 (탭을 삼키지 않는다)
  ✓ 밀어서 넘긴 동작은 타워를 선택하지 않는다
  ✓ 제자리 탭은 그대로 타워를 선택한다
  ✓ 배치 모드를 안내하고 취소 버튼으로 빠져나온다
  ✓ 판매 확인이 OS 대화상자가 아니라 게임 안 시트로 뜬다
  ✓ 진동(햅틱) 설정이 실제 vibrate 호출을 켜고 끈다
  ✓ 단일 파일 번들이 아이콘까지 온전히 실행된다
tools/subpath-check.mjs  →  통과
```

---

## 비고

### 테스트가 실제로 잡아낸 것 (2건)

**밸런스 회귀** — 21·26웨이브의 총 체력이 각각 16·21웨이브보다 낮아 난이도가 역행했다.
`content.test.mjs`의 "5웨이브 단위로 항상 증가" 검사가 걸러냈고 두 웨이브를 보강했다.

**과금 설계 위반** — "결제 없이도 모을 수 있어야 한다"고 문서에 써놓고, 실제로는 한 판을 다
깨도 캣닢이 38개뿐이라 이어하기(50) 값이 안 나왔다. `economy.test.mjs`가 잡아서 지급량을
올렸다(5웨이브당 1→2, 맵 클리어 12→20). 지금은 한 판 클리어에 52개가 모인다.

### 검증하지 못한 것 — 솔직히

- **안드로이드 Kotlin/Gradle 코드는 컴파일 검증을 못 했다.** SDK를 받을 방법이 없다.
  XML well-formed와 서브경로 서빙까지만 확인했다. AGP 8.10.1 / Kotlin 2.1.21 /
  Gradle 8.14.3 / compileSdk 35 조합도 문서 기준이지 실측이 아니다. **CI가 처음 돌아야 안다.**
- **실제 결제는 동작하지 않는다.** Play Console 계정·서명된 AAB 업로드·판매자 등록이
  전부 사람이 계정으로 해야 하는 일이라 코드로 끝낼 수 없다. 그래서 "실패를 정직하게
  드러내는 브리지"까지만 만들고 나머지는 `docs/결제연동.md`에 절차로 남겼다.
- **영수증 위조는 막지 못한다.** 진행도가 기기 로컬에만 있어 서버 검증이 없다.
  매출 규모가 생기면 계정 시스템과 서버 검증을 먼저 붙여야 한다. 이 한계를 문서에 명시했다.
- **실기기 터치감·성능은 확인 불가.** 에뮬레이터가 없다.
- **재미는 검증 대상이 아니다.** 밸런스는 수치 설계와 자동 스모크 기준이며 직접 플레이 후
  조정이 필요하다. 전부 데이터라 `content/` 안의 숫자만 고치면 된다.

### 남겨둔 확장 지점

`dot`(지속 피해) · `chain`(연쇄) · `buff`(주변 타워 강화) · `pierce`는 타워 효과 계약만,
`teleport` 같은 보스 패턴은 능력 계약만 열어두고 구현하지 않았다.
각각 `registerEffect` / `registerEnemyAbility` 한 줄로 붙일 수 있고 방법은 확장가이드에 있다.
