# Play Console 제출 팩

**이 문서는 그대로 복사해서 붙이는 용도다.** Play Console 의 각 칸에 무엇을 적을지 미리 다 써 뒀다.
값은 손으로 적은 게 아니라 코드에서 뽑았다 — 상품은 `domain/shop.js`, 확률은 `domain/gacha.js` 다.
둘 중 하나를 고치면 **이 문서도 같이 고쳐야 한다**(검사가 잡아 주지 않는 유일한 자리다).

빈칸은 딱 둘이다: **연락처 이메일**과 **출시일**. 그 둘만 정하면 나머지는 복사·붙여넣기다.

---

## 0. 먼저 — 사람만 할 수 있는 것 넷

| # | 무엇 | 왜 나는 못 하나 |
|---|---|---|
| 1 | Google Play 개발자 계정 등록 ($25, 신원 확인) | 결제·신분증이 필요하다 |
| 2 | 업로드 키(키스토어) 만들기 + GitHub 시크릿 4개 등록 | **서명 키는 당신만 가져야 한다.** 내가 만들어 채팅에 찍으면 그 순간 유출된 키다 |
| 3 | 저장소 Private 전환 | 할 수는 있지만 **지금 사이트가 죽는다** (§5 참고) |
| 4 | 실기기 플레이 | 손맛·프레임률·뒤로가기는 기기에서만 안다 |

2번 명령은 §4 에 그대로 있다.

---

## 1. 앱 기본

| 칸 | 값 |
|---|---|
| 앱 이름 | 캣포 디펜스 |
| 패키지 이름 | `com.catpaw.defense` |
| 기본 언어 | 한국어 (영어 현지화 함께 제출) |
| 앱 또는 게임 | **게임** |
| 무료 또는 유료 | **무료** (앱 내 구매 있음) |
| 카테고리 | 게임 → 전략 |
| targetSdk | 36 |
| 버전 | 1.0.0 |

## 2. 스토어 등록정보

### 간단한 설명 (80자 이내)

```
고양이들을 배치해 집으로 오는 해충을 막는 타워디펜스. 결제 없이 전부 즐길 수 있습니다.
```

### 자세한 설명

```
집으로 해충이 몰려옵니다. 고양이들을 길목에 배치해 막아 주세요.

■ 고양이 15마리, 각자 다른 일을 합니다
치즈냥은 빠르게 쏘고, 삼색냥은 광역으로 터뜨리고, 검은냥은 멀리서 저격합니다.
먼치킨냥은 해충의 장갑을 벗기고, 벵갈냥은 하늘만 봅니다.
"더 센 고양이"가 아니라 "다른 고양이"입니다 — 어떤 고양이도 다른 고양이의 상위 호환이 아닙니다.

■ 모드 여섯
· 시나리오 24장 — 컷신으로 이어지는 캠페인. 장마다 목표와 별
· 자유 모드 6맵 — 다 막으면 무한 모드로 표 밖까지
· 도전 7종 — 골드 절반, 공중만, 맨손, 목발 없이…
· 주간 도전 — 같은 주엔 모두가 같은 판
· 속성 원정 — 여섯 칸을 목숨 하나로. 덱은 네 마리
· 훈련 — 캣닢으로 고양이를 영구히 키웁니다

■ 속성 여섯이 고리를 이룹니다
흙 → 번개 → 얼음 → 불 → 어둠 → 빛 → 흙. 각 속성은 다음 속성에게 강합니다.
상성은 원정 모드에서만 걸립니다 — 맵을 깨고 나가는 길이 수집에 걸리지 않게 했습니다.

■ 결제 없이 끝까지 갑니다
자유 모드 6맵, 시나리오 1~2막, 도전 7종, 펫, 훈련, 무한, 주간, 속성 원정 — 전부 무료입니다.
유료는 그 위에 얹는 것뿐입니다: 시나리오 3막, 도전 팩 2, 스킨 팩, 캣닢 충전.
유료 콘텐츠가 무료 콘텐츠를 잠그지 않습니다.

■ 인터넷 없이 됩니다
서버가 없습니다. 진행도는 기기 안에만 저장되고, 아무것도 수집하지 않습니다.
```

### 그래픽 자산

| 자산 | 어디 | 규격 |
|---|---|---|
| 앱 아이콘 | `android/app/src/main/res/mipmap-*` | 512×512 (Play 는 별도 업로드) |
| 기능 그래픽 | `store/feature-graphic.png` | 1024×500 |
| 스크린샷 | `store/*.png` 8장 | 1080×1920 (9:16) |

`node tools/store-assets.mjs` 로 다시 굽는다.

## 3. 앱 내 구매 상품 8개

Play Console → 수익 창출 → 인앱 상품. **상품 ID 는 코드와 글자 하나까지 같아야 한다**(`domain/shop.js`).

| 상품 ID | 유형 | 가격 | 이름 | 설명 |
|---|---|---|---|---|
| `catnip_small` | 소비성 | ₩1,200 | 캣닢 한 봉지 | 캣닢 100 |
| `catnip_large` | 소비성 | ₩5,900 | 캣닢 한 자루 | 캣닢 600 |
| `starter` | 비소비성 | ₩2,900 | 스타터 팩 | 캣닢 300 + 펫 부엉이 + 스킨 1종 (1회) |
| `premium` | 비소비성 | ₩4,900 | 프리미엄 팩 | 시작 골드 +200 · 캣닢 2배 · 주간 보상 2배 · 훈련 20% 할인 |
| `act3` | 비소비성 | ₩3,900 | 시나리오 3막 | 19~24장 |
| `challenges2` | 비소비성 | ₩2,900 | 도전 팩 2 | 도전 5종 |
| `skins1` | 비소비성 | ₩2,900 | 스킨 팩 1 | 고양이 스킨 3종 |
| `skins2` | 비소비성 | ₩2,900 | 스킨 팩 2 | 고양이 스킨 3종 |

가격은 자리표시자다 — Play 가 지역별 실제 가격을 준다.

## 4. 확률형 아이템 확률 공개 — **필수**

캣닢을 현금으로도 사고 그 캣닢으로 뽑기를 돌리므로 이 게임은 확률형 아이템을 포함한다.
**한국 게임산업법이 확률 공개를 의무화한다(2024-03 시행).** 게임 안에는 이미 표가 떠 있고
(타이틀 → 뽑기), 스토어 설명과 광고물에도 같은 값을 적어야 한다.

### 스토어 설명 맨 아래에 붙일 문구 (그대로 복사)

```
[확률형 아이템 정보]
이 게임에는 확률형 아이템(고양이 카드 뽑기)이 포함되어 있습니다.
뽑기 1회당 등급별 획득 확률은 다음과 같습니다.

· 전설 (새 고양이)   2.00%
· 희귀 (새 고양이)  10.00%
· 속성 룬          38.00%
· 카드 조각        50.00%

· 10회 연속 뽑기에는 전설 또는 희귀가 1개 이상 확정 지급됩니다.
· 중복된 고양이 카드는 카드 조각 40개로 전환됩니다.
· 카드 조각 300개로 원하는 고양이 카드를 직접 교환할 수 있습니다.
· 속성 룬은 6종(흙·번개·얼음·불·어둠·빛)이 균등하게 나옵니다.
· 뽑기에 쓰는 티켓은 현금으로 판매하지 않으며, 출석과 원정 보상으로만 획득합니다.
· 위 확률은 게임 내 뽑기 화면에서도 동일하게 표시됩니다.
```

**이 숫자는 `domain/gacha.js` 의 `GACHA_TABLE` 에서 나온 것이다.** 표를 고치면 여기도 고쳐야 한다.
게임 안 표는 `disclosureRows()` 가 자동으로 만들므로 저절로 맞고, 검사가 20만 번 굴려 실측까지 대조한다.

## 5. 콘텐츠 등급 설문 (IARC)

| 문항 | 답 |
|---|---|
| 폭력 | **없음** — 만화풍 해충 퇴치, 유혈·부상 묘사 없음 |
| 성적 콘텐츠 | 없음 |
| 언어 | 없음 |
| 통제 물질 | 없음 |
| 도박 (실제 도박) | **없음** |
| **확률형 아이템 / 무작위 아이템 구매** | **있음** — 캣닢(유료 재화)으로 무작위 아이템 획득 가능 |
| 사용자 간 상호작용 | **없음** — 서버가 없어 채팅·순위표·소셜 기능이 전혀 없음 |
| 위치 공유 | 없음 |
| 개인정보 공유 | 없음 |
| 디지털 구매 | **있음** |

## 6. 데이터 안전 양식

**핵심: 이 앱은 아무것도 수집하지 않는다.** 서버가 없고 네트워크 호출도 없다.

| 문항 | 답 |
|---|---|
| 데이터를 수집하거나 공유합니까? | **아니요** |
| 데이터가 전송 중에 암호화됩니까? | 해당 없음 (전송하는 데이터가 없음) |
| 사용자가 데이터 삭제를 요청할 수 있습니까? | 해당 없음 (수집하지 않음) — 앱 삭제 시 기기 내 진행도도 함께 삭제 |
| 광고 ID 사용 | **아니요** |
| 제3자 SDK | **없음** (의존성 0) |

증거: `web/js` 전체에 서버 통신 코드가 없고, 진행도는 `localStorage` 에만 저장된다.
결제는 Google Play Billing 만 사용하며, 그것도 아직 `BillingBridge` 미구현 상태라 APK 에서는
**정직하게 실패한다**(`not_configured`).

## 7. 개인정보처리방침

Play 는 URL 을 요구한다. 기계는 준비돼 있고 **빈칸 둘만 채우면 된다**:

```bash
# 1) docs/개인정보처리방침.md 의 [연락처] 와 [출시일] 을 채운다
# 2) 변환 — 자리표시자가 남아 있으면 도구가 거부한다(exit 1)
node tools/site-build.mjs --privacy
# 3) 푸시하면 pages.yml 이 올린다
```

주소: `https://kimd10041004-gif.github.io/-catpaw-defense/privacy.html`

## 8. 업로드 키 만들기 (당신만 해야 한다)

**내가 만들면 안 된다.** 서명 키는 앱의 신원이고, 잃어버리면 그 앱을 영영 업데이트할 수 없다.
채팅에 찍힌 키는 유출된 키다. 아래를 당신 컴퓨터에서 직접 돌린다.

```bash
keytool -genkeypair -v \
  -keystore catpaw-upload.jks \
  -alias catpaw \
  -keyalg RSA -keysize 4096 -validity 10000 \
  -storetype PKCS12
# 비밀번호를 두 번 묻는다. 잊지 말고 어딘가에 안전하게 적어 둔다.

# GitHub 시크릿에 넣을 base64
base64 -w0 catpaw-upload.jks > catpaw-upload.jks.b64   # macOS 는 `base64 -i` 로
```

GitHub → 저장소 → Settings → Secrets and variables → Actions → New repository secret 로 **넷**:

| 시크릿 이름 | 값 |
|---|---|
| `CATPAW_KEYSTORE_B64` | `catpaw-upload.jks.b64` 파일 내용 전체 |
| `CATPAW_KEYSTORE_PW` | 키스토어 비밀번호 |
| `CATPAW_KEY_ALIAS` | `catpaw` |
| `CATPAW_KEY_PW` | 키 비밀번호 |

넣고 나서 CI 를 한 번 돌리면 AAB 가 **진짜 키로** 서명된다. 넣기 전에는 디버그 키로 서명되고,
Play 는 그걸 거부한다(`build.gradle.kts` 의 폴백이 그렇게 돼 있다 — 빌드는 되지만 올릴 수는 없다).

`catpaw-upload.jks` 는 **저장소에 커밋하지 않는다.** `.gitignore` 에 이미 `*.jks` 가 있다.

## 9. 저장소 Private 전환 — 지금은 하지 마라

원래 계획은 Private 이었다. 그런데 지금 바꾸면 **사이트와 아이폰 데모가 죽는다**:

- GitHub Pages 는 private 저장소에서 **GitHub Pro 이상**을 요구한다 → `…github.io/-catpaw-defense/` 와
  `/play/` 가 404 가 된다. 지금 아이폰으로 이 게임을 할 수 있는 유일한 길이 그것이다
- 릴리스 자산(APK)도 로그인해야 받아진다 → 사이트의 다운로드 버튼이 남에게 안 열린다

**Play 출시 자체에는 저장소 공개 여부가 아무 상관이 없다.** 그러니 순서는:

1. 지금은 public 으로 두고 Play 출시를 끝낸다
2. 출시 뒤 사이트가 더 필요 없어지면 그때 Private 으로 바꾼다
3. 사이트를 유지하면서 Private 으로 가고 싶으면 Vercel 로 옮긴다 — `vercel.json` 이 이미 준비돼 있다
   (같은 빌드 명령·같은 검사)

## 10. 출시 직전 마지막 확인

```bash
npm test                                          # 564 초록
NODE_PATH=/opt/node22/lib/node_modules node tools/screenshot.mjs   # 스모크 · 콘솔 에러 0
node tools/i18n-scan.mjs                          # 0건
node tools/bump-version.mjs patch                 # 배포마다 최소 patch — 안 올리면 PWA 가 안 갱신된다
```

그리고 **기기에서 직접**: 설치 → 첫 판 → 뒤로가기 → 홈으로 갔다 돌아오기 → 소리 → 진동 →
결제 버튼이 정직하게 실패하는지. 이건 내가 못 하는 유일한 검사다([`실기기설치.md`](실기기설치.md)).
